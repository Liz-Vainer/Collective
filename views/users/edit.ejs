<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
</head>
<body>
    <h1>Edit user</h1>
    <form action="/users/<%=user.id%>?_method=PUT" method="POST">
        <label for="fname">First name:</label>
        <input type="text" name="name" placeholder="first name" id="fname" value="<%=user.name%>">
        <label for="email">Email:</label>
        <input type="text" name="email" placeholder="email" id="email" value="<%=user.email%>">
        <label for="password">Password:</label>
        <input type="password" name="password" placeholder="password" id="password" value="<%=user.password%>">
        <label for="category">Category:</label>
        <select name="category" id="category">
            <% for(let category of categories){%>
                <option value="<%=category%>" <%= user.category === category ? 'selected':'' %>><%=category%></option>
            <%}%>
        </select>
        <button>Update User</button>
    </form>
    <a href="/users">Home</a>
</body>
</html>